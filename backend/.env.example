SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_JWT_SECRET=        # Project Settings → API → JWT Secret in Supabase dashboard
ANTHROPIC_API_KEY=
CORS_ORIGINS=["http://localhost:3000"]
APP_ENV=development         # Set to "production" to enforce auth on all requests
